import React from 'react';
import { InteractionTable } from '@/components/pages/interaction/InteractionTable';

function page() {
    return (
        <div>
            <InteractionTable />
        </div>
    );
}

export default page;
